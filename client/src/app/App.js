import React from 'react'

export default function App() {
  return (
	 <div>

	 </div>
  )
}



// const callApi = async () => {
// 	const response = await fetch('/api/users');
// 	const body = await response.json();
// 	if (response.status !== 200) throw Error(body.message);

// 	return body;
// };

// callApi().then(body => console.log(body))
